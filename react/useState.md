# React- useState

***UuseState→*** mecanismo que permite que o componente funcione e tenha memória e reaja a mudanças

```jsx
const [state, setState] = useState("valorInicial");
```

state→ valor atual

setState→ função que atualiza o valor atual

valorinicial→ estado na primeira renderização